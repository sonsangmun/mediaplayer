apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "com.mediaplayer.sonsangmun.mediaplayer"
        minSdkVersion 15
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            signingConfigs {
                release {
                    storeFile file("/Users/sangmun/Google/Document/Android/release key/release_key.jks")
                    storePassword "dPwls2009"
                    keyAlias "irin"
                    keyPassword "dPwls2009"
                }
            }
            debuggable false        // debugging 허용안함
            zipAlignEnabled true    // apk 최적화, zipAlign is deprecated
            minifyEnabled true      // runProguard is deprecated
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:22.2.0'
}